class Solution {

    isAnagram(s, t) {
        let sort1 = s.split("").sort().join("")
        let sort2 = t.split("").sort().join("")
        return sort1 === sort2



        // if(s.length !== t.length) {
        //     return false
        // }

        // let first = new Map()
        // let second = new Map()

        // for (let )

    }
}
